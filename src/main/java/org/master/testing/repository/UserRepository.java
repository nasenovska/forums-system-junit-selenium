package org.master.testing.repository;

import org.master.testing.entity.User;

/**
 * User repository - a central place (as a storage) in which an aggregation of {@link User} is kept and maintained.
 */
public interface UserRepository extends BaseRepository<User> {
}
